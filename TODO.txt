


- Create a store interface
- Implement filestore, memstore
- move meta to it's own file


- Missing some 'Resolves' in trie

- Store needs the hashFn

- Trie needs a cleaner api for use:

- Trie needs remove

t := UrkelTree(dir, hashFn)
t.Open()

// Write
tx := t.Transaction()
tx.Set(...)
tx.Remove()
tx.Commit()

// Read
snap := t.Snapshot()
snap.Get(...)
snap.Proof(...)